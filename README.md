# ⚛️ React + TypeScript: Team Showcase Project

Учебный проект в рамках освоения React. Фокус на изучении компонентной архитектуры, типизации пропсов и эффективного рендеринга списков.

## 🚀 О проекте
Это SPA-приложение (Single Page Application), которое отображает список карточек сотрудников. Проект построен с использованием современной сборки **Vite** и строго типизирован на **TypeScript**.

### Основные изученные концепции:
- **Component-Based Architecture**: Разделение интерфейса на независимые, переиспользуемые блоки.
- **JSX & TSX**: Написание разметки внутри JavaScript с поддержкой типов.
- **Props & Interfaces**: Безопасная передача данных от родительских компонентов к дочерним.
- **One-way Data Flow**: Понимание того, как данные текут сверху вниз.
- **Rendering Lists**: Использование метода `.map()` и важность атрибута `key`.
- **Code Refactoring**: Вынос стилей в CSS-модули и типизация данных в отдельные файлы (`types.ts`).

## 🛠️ Стек технологий
- **React 18** (Functional Components)
- **TypeScript** (Interfaces, FC types)
- **Vite** (Build Tool)
- **CSS3** (Flexbox, Grid)
- **Git** (Version Control)

## 📂 Структура проекта
```
src/
├── components/
│   ├── UserCard.tsx      # Компонент карточки (Logic + JSX)
│   └── UserCard.css      # Стили компонента
├── types.ts              # Глобальные интерфейсы данных
├── App.tsx               # Главный компонент (Архитектор страницы)
└── main.tsx              # Точка входа в приложение
```
## ⚙️ Установка и запуск <br>

 1.Клонируйте репозиторий:
 ```bash
 git clone [https://github.com/LGalushka/react-components-base.git](https://github.com/LGalushka/react-components-base.git)
 ```

 2.Перейдите в папку проекта:
 ```bash
 cd my-react-app
 ```
 
3.Установите зависимости:
```bash
npm install
```
4.Запустите проект в режиме разработки:
```bash
npm run dev
```

